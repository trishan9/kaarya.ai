services:
  api:
    image: ghcr.io/trishan9/kaarya-backend:latest
    container_name: kaarya-api
    env_file:
      - .env
    environment:
      NODE_ENV: production
      APP_PORT: ${APP_PORT:-3000}
    ports:
      - '${APP_PORT:-3000}:${APP_PORT:-3000}'
    restart: unless-stopped